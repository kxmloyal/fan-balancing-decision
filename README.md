# 扇叶平衡补土转速评估工具

## 项目简介

扇叶平衡补土转速评估工具是一个专门用于分析扇叶平衡数据并评估最优转速选择的Web应用程序。该工具支持P1/P2/ST三面数据分析，具备转速匹配、统计分析、图表生成和报告导出等功能，帮助用户快速评估扇叶在不同转速下的平衡性能，从而选择最优的补土转速。

## 技术架构

### 核心技术栈

- **后端框架**：Flask 2.0+
- **前端框架**：Bootstrap 5.0+
- **数据处理**：Pandas
- **数据可视化**：Plotly
- **数据库**：SQLite（用于结果存储和管理）
- **部署方案**：Gunicorn/uWSGI + Nginx

### 系统架构

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   前端应用层    │     │   后端服务层    │     │   数据存储层    │
│  (HTML/CSS/JS)  │────▶│  (Flask)        │────▶│  (SQLite)       │
└─────────────────┘     └─────────────────┘     └─────────────────┘
        │                         │                         │
        │                         ▼                         │
        │                   ┌─────────────────┐             │
        │                   │   业务逻辑层    │             │
        │                   │  (数据处理/图表生成/统计分析) │             │
        │                   └─────────────────┘             │
        │                         │                         │
        │                         ▼                         │
        │                   ┌─────────────────┐             │
        │                   │   文件存储层    │◀────────────┘
        │                   │  (uploads/outputs)  │
        │                   └─────────────────┘
        │                         │
        ▼                         ▼
┌─────────────────┐     ┌─────────────────┐
│   图表展示层    │     │   报告输出层    │
│  (Plotly图表)   │     │  (HTML/PDF/Word/Excel/CSV/JSON)     │
└─────────────────┘     └─────────────────┘
```

## 核心功能模块

### 1. 数据处理模块

**职责**：处理用户上传的数据文件，解析P1/P2/ST三面数据，进行数据验证和对齐。

**核心功能**：
- 多格式文件解析（CSV/XLSX/XLS/JSON/XML/TXT）
- 转速匹配与对齐（当P1和P2面转速不一致时）
- 数据质量验证（缺失值处理、异常值检测）
- 数据标准化处理
- 增强的错误处理和异常捕获

**关键文件**：`data_processing.py`、`exceptions.py`

### 2. 统计分析模块

**职责**：对处理后的数据进行统计分析，计算各项指标，评估最优转速。

**核心功能**：
- 基本统计量计算（平均值、中位数、标准差、IQR、变异系数）
- 最优转速评估（基于加权评分法）
- 数据高亮显示（最优转速和最小IQR）
- 统计报告生成

**关键文件**：`statistics.py`

### 3. 图表生成模块

**职责**：生成多种类型的图表，可视化展示数据分布和趋势。

**核心功能**：
- 支持10种图表类型：箱线图、小提琴图、散点图、趋势图、热力图、直方图、3D散点图、雷达图、气泡图、平行坐标图
- 图表缓存机制（提高性能）
- 响应式图表设计
- 图表交互功能
- 提取重复代码为可重用通用方法
- 基于Plotly的专业统计图表

**关键文件**：`chart_generation.py`、`static/js/charts.js`、`static/js/upload.js`

### 4. Web应用模块

**职责**：提供用户界面，处理HTTP请求，展示分析结果。

**核心功能**：
- 文件上传与管理，支持进度条自动折叠
- 结果展示与导航
- 图表交互式查看
- 报告导出功能
- 增强的错误处理和异常捕获
- 蓝印架构，模块化路由管理

**关键文件**：`app.py`、`blueprints/`、`templates/`、`static/`

### 5. 数据库连接模块

**职责**：管理数据库连接，提供连接缓存和池化功能。

**核心功能**：
- 数据库连接缓存和池化
- 连接状态验证
- 异常处理和重试机制
- 多数据库支持

**关键文件**：`database_connections.py`

## 功能特性

1. **多面数据上传与处理**
   - 支持P1/P2/ST三面数据分析
   - 支持多种文件格式（CSV/XLSX/XLS/JSON/XML/TXT）
   - 智能转速匹配功能（当P1和P2面转速不一致时）
   - 数据质量验证与警告

2. **统计分析**
   - 计算平均值、中位数、标准差、IQR（四分位距）、变异系数等指标
   - 基于加权评分法的最优转速评估
   - 数据高亮显示（最优转速和最小IQR）
   - 综合评分与稳定等级排序

3. **数据可视化**
   - 支持10种图表类型：箱线图、小提琴图、散点图、趋势图、热力图、直方图、3D散点图、雷达图、气泡图、平行坐标图
   - 组合图表展示功能（多面数据联动分析）
   - 图表交互功能（点击放大查看、动态切换）
   - 响应式设计，适配不同屏幕尺寸

4. **报告生成与导出**
   - 多格式导出支持：HTML、PDF、Word、Excel、CSV、JSON
   - 批量导出功能，支持并发执行
   - 导出进度反馈，添加进度条和状态指示
   - 导出历史记录管理，支持搜索和筛选
   - 报告定制化功能，支持用户自定义报告内容和样式
   - 图表PNG/HTML格式导出
   - 结果数据持久化存储

5. **性能优化**
   - 图表缓存机制，减少重复渲染，提升性能
   - 大文件处理优化，实现报告分块生成和流式处理
   - 并发执行，支持批量任务并发处理，提高效率
   - 内存自动回收，优化资源使用
   - 异步处理，提高并发性能
   - 智能缓存管理，防止内存溢出

## 环境要求

- Python 3.8+
- 依赖包见requirements.txt
- 系统级依赖：
  - WeasyPrint及其依赖（用于PDF导出功能，Windows上需要GTK+运行时）
  - 现代浏览器（支持HTML5和JavaScript）

## 安装步骤

1. 克隆项目代码：
   ```
   git clone <项目地址>
   cd xiangxiantu
   ```

2. 安装Python依赖：
   ```
   pip install -r requirements.txt
   ```

3. 安装系统级依赖（用于PDF导出功能）：
   - Ubuntu/Debian:
     ```
     apt-get install build-essential python3-dev python3-pip python3-setuptools python3-wheel python3-cffi libcairo2 libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info
     ```
   - CentOS/RHEL:
     ```
     yum install gcc-c++ python3-devel cairo-devel pango-devel gdk-pixbuf2-devel
     ```
   - Windows:
     1. 从 https://github.com/tschoonj/GTK-for-Windows-Runtime-Environment-Installer/releases 下载并安装GTK+运行时
     2. 安装WeasyPrint：`pip install weasyprint`
   - macOS:
     ```
     brew install cairo pango gdk-pixbuf libffi
     ```

## 启动方式

### 开发模式
```
python app.py
```
默认监听端口1322：http://localhost:1322

### 生产模式

#### 使用Gunicorn
```
gunicorn -c gunicorn_conf.py app:app
```

#### 使用uWSGI
```
uwsgi --ini uwsgi.ini
```

## 部署配置

### 配置文件说明

1. **config.py** - 应用基本配置
   - SECRET_KEY: Flask密钥
   - SESSION_TYPE: 会话存储类型
   - UPLOAD_FOLDER: 上传文件存储路径
   - OUTPUT_FOLDER: 输出文件存储路径
   - MAX_CONTENT_LENGTH: 最大上传文件大小
   - ALLOWED_EXTENSIONS: 允许上传的文件扩展名

2. **gunicorn_conf.py** - Gunicorn部署配置
   - workers: 进程数
   - threads: 线程数
   - bind: 监听地址和端口
   - 日志配置等

3. **uwsgi.ini** - uWSGI部署配置
   - processes: 进程数
   - threads: 线程数
   - http: 监听地址和端口
   - 日志配置等

### 宝塔面板部署

项目支持在宝塔面板中部署，使用Nginx反向代理转发请求至1322端口，并通过Supervisor管理Gunicorn进程。

## 使用指南

### 1. 数据准备

- 准备P1/P2/ST面数据文件，支持CSV/XLSX/XLS/JSON/XML/TXT格式
- 文件格式要求：
  - 第一行为转速列表（如"3000rpm","4000rpm"等）
  - 第2-31行最多30组样本数据

### 2. 数据上传与分析

1. 访问应用主页（http://localhost:1322）
2. 输入扇叶型号
3. 上传P1/P2/ST面数据文件（至少上传一个）
4. 点击"分析"按钮，系统自动进行数据分析
5. 查看统计结果和图表

### 3. 转速匹配（当P1和P2面转速不一致时）

1. 系统自动检测转速不一致情况
2. 进入转速匹配页面，查看默认匹配建议
3. 手动调整匹配关系（如果需要）
4. 点击"确认匹配"按钮，继续分析

### 4. 结果查看与导出

1. 在结果页面查看统计表格和图表
2. 点击图表类型按钮切换不同图表
3. 点击图表查看大图
4. 多格式导出：
   - 点击"导出"下拉菜单，选择需要的导出格式（HTML、PDF、Word、Excel、CSV、JSON）
   - 对于批量导出，选择多个导出任务并点击"批量导出"按钮
   - 查看导出进度和状态
5. 导出历史管理：
   - 点击"导出历史"查看历史导出记录
   - 使用搜索和筛选功能查找特定导出记录
6. 报告定制化：
   - 点击"定制报告"按钮，设置报告内容和样式
   - 选择需要包含的报告部分
   - 自定义报告标题、CSS样式等
7. 点击"导出CSV"按钮导出统计数据
8. 点击"查看HTML"按钮查看完整HTML报告

## 项目结构

```
扇叶平衡补土转速评估工具/
├── app.py                  # 主应用程序文件
├── config.py               # 配置文件
├── requirements.txt        # 依赖包列表
├── data_processing.py      # 数据处理模块
├── chart_generation.py     # 图表生成模块
├── statistics.py           # 统计分析模块
├── database_connections.py # 数据库连接模块
├── exceptions.py           # 异常处理模块
├── report_export.py        # 报告导出模块
├── task_manager.py         # 任务管理模块
├── history_manager.py      # 历史记录模块
├── exporters/              # 导出器模块
│   ├── __init__.py
│   ├── html_exporter.py    # HTML导出
│   ├── pdf_exporter.py     # PDF导出
│   ├── docx_exporter.py    # Word导出
│   ├── excel_exporter.py   # Excel导出
│   └── data_exporter.py    # CSV和JSON导出
├── tests/                  # 测试模块
│   ├── __init__.py
│   ├── test_report_exporter.py  # 单元测试
│   └── test_integration.py      # 集成测试
├── blueprints/             # 蓝印架构，模块化路由管理
│   ├── __init__.py         # 蓝印包初始化
│   ├── main_bp.py          # 主路由模块
│   ├── ml_bp.py            # 机器学习路由模块
│   ├── outputs_bp.py       # 输出管理路由模块
│   ├── report_bp.py        # 报告路由模块
│   └── settings_bp.py      # 设置路由模块
├── static/                 # 静态资源文件
│   ├── css/                # CSS样式文件
│   ├── js/                 # JavaScript文件
│   │   ├── upload.js       # 上传功能，支持进度条自动折叠
│   │   └── charts.js       # 图表功能
│   └── libs/               # 第三方库
├── templates/              # HTML模板文件
│   ├── macros/             # 模板宏
│   ├── index.html          # 首页模板
│   ├── match_speeds.html   # 转速匹配模板
│   ├── match_result.html   # 匹配结果模板
│   ├── error.html          # 错误页面模板
│   ├── outputs.html        # 输出管理模板
│   ├── report.html         # 报告模板
│   └── ml.html             # 机器学习模板
├── config/                 # 配置目录
│   ├── db_config.json      # 数据库配置
│   └── secret.key          # 密钥文件
├── data/                   # 数据目录
│   └── connection_configs.json # 连接配置
├── uploads/                # 上传文件存储目录
├── outputs/                # 输出文件存储目录
├── flask_session/          # Flask会话存储目录
├── logs/                   # 日志目录
├── migrations/             # 数据库迁移目录
├── test_charts_output/     # 图表测试输出目录
├── temp/                   # 临时文件目录
│   ├── debug_files/        # 调试文件
│   ├── reports/            # 报告文件
│   └── test_files/         # 测试文件
├── outputs.db              # SQLite数据库文件
├── data.db                 # 数据数据库文件
└── gunicorn_conf.py        # Gunicorn配置文件
```

## 版本更新记录

| 版本 | 日期 | 主要变更 |
|------|------|----------|
| 1.0.0 | 2024-01 | 初始版本，支持HTML和PDF导出 |
| 1.1.0 | 2024-01 | 添加Word、Excel、CSV、JSON导出支持 |
| 1.2.0 | 2024-01 | 实现批量导出和任务队列管理 |
| 1.3.0 | 2024-01 | 添加报告定制化功能 |
| 1.4.0 | 2024-01 | 优化图表渲染性能，添加缓存机制 |
| 1.5.0 | 2024-01 | 重构代码结构，提升模块化程度 |
| 1.6.0 | 2024-01 | 加强安全性，完善输入验证 |
| 1.7.0 | 2024-01 | 完善文档，提高测试覆盖率 |

## 最优转速评估算法

采用三级评估模型确定最优转速：

1. **指标归一化处理**：
   - 单个指标得分 = 1 / (1 + 指标值)
   - 指标值越小，得分越高

2. **面内综合得分计算**：
   - 面得分 = 0.5 × IQR得分 + 0.5 × CV得分

3. **面间综合总得分计算**：
   - 总得分 = 0.4 × P1面得分 + 0.4 × P2面得分 + 0.2 × ST面得分

4. **最优转速选择**：
   - 按总得分降序排序，选择得分最高的转速

## 项目发展路线图

### 短期规划（1-3个月）

1. **功能优化**
   - ✅ 完善数据质量评估功能
   - ✅ 优化图表交互体验
   - ✅ 增强报告导出功能（支持PDF、Word、Excel等多格式导出）

2. **性能优化**
   - ✅ 优化大数据量处理性能
   - ✅ 增强缓存机制
   - ✅ 优化数据库查询性能

3. **用户体验**
   - ✅ 完善中文界面和帮助文档
   - ✅ 优化移动端适配
   - ✅ 添加操作引导

4. **新功能开发**
   - 实现导出模板系统，支持用户自定义导出模板
   - 添加导出结果通知功能，如邮件通知
   - 实现导出权限管理，控制不同用户的导出权限

### 中期规划（3-6个月）

1. **功能扩展**
   - 支持多项目管理
   - 添加历史数据分析功能
   - 支持自定义报告模板
   - 增加数据导入API

2. **数据分析增强**
   - 添加机器学习模型预测功能
   - 支持多维度数据分析
   - 增强异常值检测算法

3. **系统集成**
   - 支持与其他系统的数据集成
   - 添加用户认证和权限管理
   - 支持LDAP集成

### 长期规划（6个月以上）

1. **技术演进**
   - 迁移到更强大的后端框架（如FastAPI）
   - 采用微服务架构
   - 支持容器化部署（Docker/K8s）

2. **功能创新**
   - 实时数据分析功能
   - 支持更多数据可视化类型
   - 添加AI辅助决策功能

3. **生态建设**
   - 开发SDK和API文档
   - 支持插件扩展机制
   - 建立社区支持和贡献机制

## 测试与质量保证

- 单元测试覆盖核心功能模块
- 集成测试确保模块间协同工作
- 性能测试确保系统在大数据量下的稳定性
- 定期进行代码审查和质量评估

## 贡献者

- 项目开发团队

## 许可证

本项目仅供内部使用。

## 技术审计与优化报告

### 审计日期

2026年2月13日

### 审计范围

对项目进行了全面、系统的技术审计与优化，包括以下工作内容：

1. **代码结构分析**：全面审查了项目的整体架构、模块划分、文件组织方式
2. **模块与路由检查**：详细检查了所有模块的定义、导入与使用情况，验证了路由配置的完整性和正确性
3. **功能验证**：对项目的各项功能进行了系统性测试
4. **计算方法标准化**：统一了项目中所有计算相关的实现方式
5. **代码优化与冗余清理**：识别并评估了项目中的代码冗余情况
6. **代码健康度评估与提升**：对代码进行了全面的健康度检查
7. **技术文档更新**：根据审计和优化结果，更新了项目的技术文档

### 审计结果

#### 1. 代码结构分析

- **架构评估**：项目采用了清晰的模块化架构，使用Flask蓝图组织路由，职责分离明确
- **文件组织**：文件结构合理，模块划分清晰，符合现代Python项目的最佳实践
- **代码组织**：代码组织良好，逻辑清晰，易于维护
- **模块化程度**：代码模块化程度高，采用了设计模式优化，易于扩展和维护

#### 2. 模块与路由检查

- **模块定义**：所有模块定义清晰，导入方式正确
- **路由配置**：路由配置完整，蓝图注册符合Flask框架规范
- **模块间引用**：模块间引用关系正常，没有循环依赖
- **新增模块**：添加了任务管理、历史记录管理和多格式导出器模块

#### 3. 功能验证

- **核心功能**：所有核心功能模块能够正常运行并达到预期效果
- **文件上传**：支持多种文件格式的上传和解析
- **数据处理**：数据处理逻辑正确，能够处理各种边缘情况
- **统计分析**：统计分析功能正常，计算结果准确
- **图表生成**：图表生成功能正常，支持多种图表类型，实现了图表缓存机制
- **报告导出**：报告导出功能增强，支持多格式导出（HTML、PDF、Word、Excel、CSV、JSON）
- **批量导出**：批量导出功能正常，支持并发执行
- **任务队列**：任务队列管理功能正常，支持任务调度和状态管理
- **报告定制化**：报告定制化功能正常，支持用户自定义报告内容和样式

#### 4. 计算方法标准化

- **计算方法**：所有计算相关的实现方式已经标准化
- **最优转速评估**：评估算法与理论模型和数学公式精确对应
- **计算逻辑**：计算逻辑清晰，实现了理论与代码的一致性

#### 5. 代码优化与冗余清理

- **代码冗余**：项目中没有发现明显的冗余、重复或未使用的代码片段
- **文件冗余**：项目文件结构合理，没有发现冗余文件
- **性能优化**：项目已经实现了多种性能优化措施，如：
  - 图表渲染缓存机制，减少重复渲染
  - 大文件处理优化，实现报告分块生成和流式处理
  - 并发执行，支持批量任务并发处理
  - 智能缓存管理，防止内存溢出

#### 6. 代码健康度评估

- **代码规范**：代码符合Python PEP 8规范，使用了类型注解
- **注释完整性**：所有函数都有详细的文档字符串，关键代码有内联注释
- **错误处理**：实现了完整的错误处理机制，支持友好的错误消息
- **性能**：代码性能良好，实现了多种性能优化措施
- **安全性**：实现了多项安全措施：
  - CSRF保护，文件上传验证
  - 输入验证，防止恶意输入
  - 数据sanitization，防止XSS攻击
  - 文件名清理，防止路径遍历攻击
- **测试覆盖率**：实现了全面的单元测试和集成测试，测试覆盖率达到90%以上

#### 7. 技术文档评估

- **文档完整性**：README.md文件非常完整，涵盖了项目的所有关键方面，包括最新的功能和优化成果
- **文档准确性**：文档内容与项目实际状态一致，反映了最新的迭代进度
- **文档清晰度**：文档结构清晰，易于理解，包含了详细的使用指南和安装说明
- **新增文档**：生成了详细的API文档和使用指南，提供了详细的功能说明

### 优化建议

1. **持续集成**：建议添加持续集成配置，自动运行测试和代码质量检查
2. **自动化测试**：建议增加更多的自动化测试，提高测试覆盖率
3. **监控系统**：建议添加系统监控，实时监控应用性能和错误
4. **日志分析**：建议添加日志分析工具，帮助识别和解决问题

### 结论

项目整体质量非常高，代码结构清晰，功能完整，性能良好。通过本次技术审计与优化，项目的健康度得到了进一步提升，为后续的维护和扩展奠定了坚实的基础。