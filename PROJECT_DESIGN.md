# 扇叶平衡补土转速评估工具 - 项目设计大纲

## 1. 项目概述

扇叶平衡补土转速评估工具是一个专门用于分析扇叶平衡数据并评估最优转速选择的Web应用程序。该工具支持P1/P2/ST三面数据分析，具备转速匹配、统计分析、图表生成和报告导出等功能。

### 1.1 项目目标
- 分析扇叶在不同转速下的平衡数据
- 通过统计学方法评估最优运行转速
- 提供可视化图表展示分析结果
- 生成详细的分析报告供决策参考

### 1.2 核心功能
- 多面数据上传与处理（P1/P2/ST面）
- 转速匹配功能（当P1和P2面转速不一致时）
- 统计分析（计算平均值、中位数、标准差、IQR、变异系数等指标）
- 数据可视化（箱线图、小提琴图、散点图、趋势图等）
- 最优转速评估（基于加权评分法）
- 报告生成与导出（HTML格式）

## 2. 技术架构

### 2.1 技术栈
- 后端框架：Flask (Python)
- 前端框架：Bootstrap 5, Chart.js
- 数据处理：Pandas, NumPy
- 图表生成：Plotly
- 会话管理：Flask-Session

### 2.2 项目结构
```
扇叶平衡补土转速评估工具/
├── app.py                  # 主应用程序文件
├── config.py               # 配置文件
├── requirements.txt        # 依赖包列表
├── data_processing.py      # 数据处理模块
├── chart_generation.py     # 图表生成模块
├── statistics.py           # 统计分析模块
├── utils/                  # 工具模块
│   ├── data_validator.py   # 数据验证器
│   └── chart_cache.py      # 图表缓存管理
├── templates/              # HTML模板文件
│   ├── index.html          # 主页面模板
│   ├── match_speeds.html   # 转速匹配页面
│   ├── match_result.html   # 匹配结果页面
│   └── error.html          # 错误页面
├── static/                 # 静态资源文件
│   ├── css/                # 样式文件
│   ├── js/                 # JavaScript文件
│   └── libs/               # 第三方库
├── uploads/                # 上传文件存储目录
├── outputs/                # 输出文件存储目录
└── reports/                # 报告存储目录
```

## 3. 核心模块设计

### 3.1 主应用模块 (app.py)
- Flask应用初始化和配置
- 路由定义和请求处理
- 文件上传和处理流程
- 会话管理和数据存储
- 报告生成和导出功能

### 3.2 数据处理模块 (data_processing.py)
- 文件格式验证和支持（CSV/XLSX/XLS）
- 数据文件读取和解析
- 转速数据提取和验证
- 数据对齐和预处理

### 3.3 统计分析模块 (statistics.py)
- 统计指标计算（平均值、中位数、标准差、IQR、变异系数）
- 最优转速评估算法实现
- 统计报告生成（HTML和CSV格式）
- 数据高亮显示（最优转速和最小IQR）

### 3.4 图表生成模块 (chart_generation.py)
- 多种图表类型支持（箱线图、小提琴图、散点图、趋势图）
- 图表样式和主题配置
- 图表导出功能（PNG和HTML格式）
- 中位数曲线绘制和优化

### 3.5 工具模块 (utils/)
#### data_validator.py
- 数据验证和对齐
- 数据质量检查和警告生成
- 异常值处理

#### chart_cache.py
- 图表缓存管理
- 缓存清理和维护

### 3.6 前端模板 (templates/)
#### index.html
- 主页面布局和UI设计
- 文件上传区域
- 分析结果显示区域
- 图表展示区域
- 报告导出功能

#### match_speeds.html
- 转速匹配界面
- P1/P2面转速映射配置
- 匹配结果预览

#### match_result.html
- 转速匹配结果展示
- 匹配和未匹配数据分别显示
- 统计分析和图表展示

#### error.html
- 错误信息展示页面
- 用户友好的错误提示

## 4. 核心算法设计

### 4.1 最优转速评估算法
采用三级评估模型确定最优转速：

1. **指标归一化处理**：
   - 单个指标得分 = 1 / (1 + 指标值)
   - 指标值越小，得分越高

2. **面内综合得分计算**：
   - 面得分 = 0.5 × IQR得分 + 0.5 × CV得分

3. **面间综合总得分计算**：
   - 总得分 = 0.4 × P1面得分 + 0.4 × P2面得分 + 0.2 × ST面得分

4. **最优转速选择**：
   - 按总得分降序排序，选择得分最高的转速

### 4.2 转速匹配算法
当P1和P2面转速不一致时：
- 自动推荐匹配关系（基于转速数字相似度）
- 用户手动调整匹配关系
- 数据验证和对齐处理

## 5. 数据流程设计

### 5.1 文件上传流程
1. 用户上传P1/P2/ST面数据文件
2. 验证文件格式和内容
3. 解析转速和样本数据
4. 根据上传情况分流处理：
   - 同时上传P1/P2面：进入转速匹配流程
   - 仅上传单面：直接进入分析流程

### 5.2 转速匹配流程
1. 分析P1/P2面转速集合
2. 若转速一致：直接进入分析流程
3. 若转速不一致：
   - 自动生成推荐匹配关系
   - 用户确认或调整匹配关系
   - 数据对齐和验证
   - 进入分析流程

### 5.3 数据分析流程
1. 统计指标计算
2. 最优转速评估
3. 图表生成
4. 报告生成
5. 结果展示

## 6. 用户界面设计

### 6.1 主界面
- 文件上传区域（P1/P2/ST面）
- 分析结果展示区域
- 统计表格显示
- 图表展示区域
- 报告导出功能

### 6.2 转速匹配界面
- P1/P2面转速对照表
- 匹配关系配置控件
- 推荐匹配显示
- 匹配结果预览

### 6.3 报告展示界面
- 统计分析结果表格
- 图表展示
- 分析方法说明
- 优化建议

## 7. 部署和运维

### 7.1 环境要求
- Python 3.7+
- 依赖包见requirements.txt
- 推荐使用Gunicorn或uWSGI部署

### 7.2 配置说明
- SECRET_KEY: Flask密钥
- SESSION_TYPE: 会话存储类型
- UPLOAD_FOLDER: 上传文件存储路径
- OUTPUT_FOLDER: 输出文件存储路径
- MAX_CONTENT_LENGTH: 最大上传文件大小

### 7.3 启动方式
```bash
python app.py
```
默认监听端口1322

## 8. 扩展性和维护性

### 8.1 模块化设计
- 功能模块分离，便于维护和扩展
- 清晰的接口定义
- 松耦合的组件设计

### 8.2 可扩展功能
- 支持更多图表类型：可以扩展支持热力图、雷达图等多种数据可视化方式，满足不同场景下的数据展示需求
- 增加更多的统计分析方法：可以添加回归分析、聚类分析、时间序列分析等高级统计方法，提供更深入的数据洞察
- 支持更多文件格式：除了现有的CSV/XLSX/XLS格式，还可以扩展支持JSON、数据库导出文件等格式，提高数据导入的灵活性
- 添加用户权限管理：实现多用户系统，不同用户可以拥有不同的数据访问权限和操作权限，保障数据安全
- 增加数据导出选项：提供更多数据导出格式，如PDF报告、Excel完整报表等
- 实现实时数据处理：支持流式数据处理，实现实时分析和监控功能
- 添加自定义分析规则：允许用户自定义分析参数和权重，适应不同行业和应用场景的需求

### 8.3 维护建议
- 定期清理上传和输出目录
- 监控系统性能和资源使用
- 更新依赖包以修复安全漏洞
- 根据用户反馈优化用户体验